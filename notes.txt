REPL:
evcxr --opt 2

:dep lyza = { path = "C:/data/rust/lyza/" }
use lyza::*;
use general::*;
use market::types::*;
use market::chaintypes::*;

let hd201601 = store::histdata::load_all(2016, 1);
let ts0:Timestamp = hd201601.unders[0].0;
let xpir0:ExpirDate = hd201601.calls[0].1.opt.expir;
let chall201601:ChainsAll = store::histdata::make_chall(&hd201601);
let strikes:Vec<StrikeType> = chall201601.at(ts0).unwrap().expir(xpir0).unwrap().calls.keys().map(|x| *x).collect();


